"""
druid —— 激光剥蚀 ICP-MS（LA-ICP-MS）锆石 U-Pb 定年数据还原包
==========================================================

设计目标
--------
把一条完整的数据还原流程拆成 **职责单一、可单独测试、可单独复用** 的若干层：

    druid.core        物理/数学内核：衰变常数、标准物质参考值、年龄方程、
                    普通铅模型、统计量 —— 不依赖任何 I/O
    druid.io          输入/输出：Qtegra CSV 解析、测点序列表、Excel 报表写出、
                    给下游程序读的质控交接 JSON
    druid.reduction   单点数据还原：剥蚀区间识别、气体空白、204Hg 干扰校正、
                    普通铅校正、比值与 jackknife 不确定度
    druid.depth       深度剖面/时间分辨：滑动窗口、F(τ) 逐深度分馏校正、
                    年龄域分割（核/边识别）、图件
    druid.qc          质控判定：把「这批数据能不能用」落成带稳定 key 的检查项，
                    供下游按 key 分支（**只读不重算**）
    druid.workflow    编排层：把上面各层串成一条完整批处理流水线
    druid.cli         命令行入口：参数解析 + 调用编排层

一句话流程
----------
    Qtegra CSV
      → 自动识别剥蚀区间（扣掉未剥蚀的气体空白段与冲洗尾巴）
      → 扣气体空白
      → 204Hg 干扰校正 + 普通铅校正（带显著性检验！）
      → 主标 91500 夹逼归一化（含逐深度的分馏因子 F(τ)）
      → 外部重现性估计 + 年龄与不确定度
      → 逐点深度剖面年龄域判别
      → Excel 报表 + 剖面图件 + 质控交接 JSON

两个针对本批数据的关键设计（务必保留，删掉结果会失真）
------------------------------------------------------
1. **204 普通铅校正做显著性检验**
   204Pb 通常只有几~几十 cps，而 204Hg 本底可达数百 cps，两者做差之后
   残余常常就是噪声。若 204Pb 不显著却强行扣除，等于把噪声当成普通铅扣掉 ——
   而这份偏差几乎全落在 207Pb/206Pb 上：238U/235U ≈ 137.8（代码里是
   `core/constants.py` 的 `U238_U235`），放射成因的 207Pb 本来就比 206Pb
   少得多（不是“计数少”——见下面第 2 条）。

2. **207Pb/206Pb 不做 F(τ) 逐窗口深度校正**
   它是 Pb 同位素比值，几乎不存在随坑深的元素分馏（随坑深漂移的是 Pb/U
   这种"元素对元素"比值）。窗口级 R76 的计数统计已在 1% 量级
   （4 s 窗口的 207Pb 净计数中位约 9×10³），能指望的 Pb-Pb 漂移远小于它，
   套 F(τ) 只会注入噪声。
   ⚠ 这里曾经写"窗口级 207Pb 计数极少（仅约 130 个计数）" —— **那个数
   错安了通道**：百来个的量级属于 **204Pb**（窗口净计数中位约 37，
   5% 分位已是负数）。重算：`python tools/window_scale_facts.py`。

用法示例
--------
    # 命令行
    python -m druid.cli.reduce_batch --dir "D:/data/EX2022A" --out 结果.xlsx --plot

    # 作为库调用
    from druid.workflow import BatchConfig, run_batch
    result = run_batch(BatchConfig(data_dir="D:/data/EX2022A"))
"""
from __future__ import annotations

__version__ = "2.5.0"

# 版本说明
# --------
# 2.5.0 重构文档站（四份页面），并把「文档里的数字」纳入回归守护。
#   **不改任何数值输出**（`结果`/`标样QC`/`深度剖面域`/`不分域年龄`/`剖面窗口`
#   五张表与 2.4.0 逐位一致，端到端回归基线逐项不变）。
#   · 落地页 `docs/index.html` 按「信息优先级」重排：标题下新增一句话价值主张；
#     三条设计原则改成**判断句小标题 + 违反代价 + 展开解释**；新增独立一节
#     「先看这三个数」（91500 偏差 <1% / Ple 偏差 <3% / 协和度占比 >85%，
#     各带实测值与判据原文）；ADEPT 接口先给一句总结再上技术细节。
#   · 新增**「分域前后」三联图**：同一个示例批次里的三种剖面形态 ——
#     ① 均一（域表里根本没有它，整体积分就够）、② 核边分明（两段平台，
#     一个窗口都没被剥掉）、③ 复杂变化（三段平台 + 一片被剥掉的过渡带）。
#     由仓库内脚本 `tools/gen_docs_split_figure.py` 从 `run_batch()` 的公开表
#     算出并就地注入页面；图上每个测点的数字由 `tests/check_example_batch.py`
#     按行抓回核对。
#     **图是算出来的，不是手抄的** —— 算法一改、图忘了重新生成，测试当场红。
#   · 四份页面补 SEO/社交/可访问性：完整 meta（title/description/keywords/
#     canonical/双 theme-color）+ Open Graph + Twitter 卡；JSON-LD 结构化数据
#     （落地页 `SoftwareApplication`，三份文档页 `TechArticle` + `BreadcrumbList`）；
#     新增 `docs/robots.txt` 与 `docs/sitemap.xml`。
#   · 三份文档页补**深色模式**（`prefers-color-scheme`；只覆盖 `:root` 变量与
#     三处硬编码色，内联 SVG 一个字节没动 —— 深色下给图一块浅色面板）、
#     **窄屏响应式**（≤700px 排版收缩；宽表与宽图加横向滚动容器 + 滑动提示）、
#     **键盘可达**（`.skip` 跳转正文、`:focus-visible` 焦点环、
#     `prefers-reduced-motion` 关掉入场动画）。对比度实测：正文 14.3:1、
#     次要文字 6.6:1、最淡小字 5.1:1，全部 ≥ WCAG AA。
#   · 落地页改**深色/浅色双主题**（导航栏按钮切换 + 写 localStorage；
#     首屏内联引导脚本按"用户选择 > 系统偏好 > 深色"决定，避免闪白）。
#   · 修正**文档里的两处数字**（纯文字，不碰任何输出）。它们属于同一类错误 ——
#     数字被抄下来之后就再也没有东西能重算它：
#     ① `depth/windows.py` 曾经写"win = 4 s 约 **25~35 个 cycle** per window"，
#        实测是 **12~13 个**（cycle 周期 0.31871 s；`剖面窗口` 表的 `n_cycles`
#        列一直摆着真值 13）。
#     ② 有 **8 个文件、共 9 处**曾经写"窗口级 207Pb 只有约 130 个计数"，拿它当"不套 F(τ)"
#        的第二条理由 —— 那个数**错安了通道**：百来个的量级属于
#        **204Pb**（窗口净计数中位约 37），207Pb 实测中位 **8,790**。结论不变，
#        理由改为"计数统计已在 1% 量级，能指望的 Pb-Pb 漂移远小于它"。
#        顺带修掉同源的第三处"对计数本来就很少的 207Pb 是灾难性的"——
#        204 扣铅伤 207Pb/206Pb 是因为**放射成因的 207Pb 本来就少**
#        （238U/235U ≈ 137.8），不是因为计数少。
#     新增 `tools/window_scale_facts.py` 做可重算依据（口径写在它的 docstring：
#     净计数 = Σ(net cps) × cycle 周期）；`tests/check_example_batch.py` 新增
#     「窗口尺度」一节守着，并把那三个错串**全仓禁用**（只放行带"曾经写 /
#     错安了通道"标记的警示行）。
# 2.4.0 新增「不分域年龄」表：把整个剥蚀段当作一个域算出**一个**年龄，
#   全部样品测点走同一条处理路径 —— 跨测点横向对比从此有了统一口径。
#   · 新 sheet「不分域年龄」：每行一个样品测点（**含均一点**）。原先只有
#     「深度剖面域」表，而它只收多域点，均一点在里面没有行 ——
#     于是"分域之后仍报均一、整段却不是常数年龄"的测点根本看不见。
#     现在全部测点同表可减：不分域窗口加权平均、分域主域、结果表整段积分。
#   · 逐点剖面图多一条横贯全段的灰虚线＝不分域加权平均，与各域红线直接对照。
#   · `BatchResult.overall` / `druid.depth.domains.whole_spot_stats()`。
#     表里的数**全部由 depth 层产出**，与分域结果同一套窗口、同一个 F(τ)、
#     同一个反比方差加权口径 —— 两级之差正好是"分域"这一步的贡献，
#     中间不掺口径差异。
#   · 质控新增两条稳定 key：`samples.whole_spot`（有多少测点的整段年龄
#     可以直接用）、`samples.unresolved_structure`（分域拆不掉、但整段
#     已非常数的点）。交接 JSON 新增顶层块 `whole_spot`
#     （每测点一行 + 可机读的 `mswd_compatible`）。
#   · `mswd_acceptance()` 成为 MSWD 相容判据的**唯一定义**（原先在
#     refine_domains 与 summarize_segments 里各写了一遍）。
#   · 数值中性：只新增表与检查项，`结果`/`标样QC`/`深度剖面域`/`剖面窗口`
#     四张表的每一个数与 2.3.0 逐位一致（端到端回归基线逐项不变）。
#
# 2.3.0 起：质控从「控制台里的散文」变成**可机读的检查项 + 交接文件** ——
#   为把本工具接进自动化质控/解释流程（以及把剖面喂给 ADEPT 判坪）做准备。
#   新增两层，**不改任何数值输出**（端到端回归基线逐项不变）：
#   · `druid.qc`：`assess_batch(result)` 出 23 条带稳定 key 的检查项
#     （分 fail/warn/info/pass 四级），全部阈值集中在 `QCThresholds`，
#     可由 `BatchConfig.qc_thresholds` 覆盖。它**只读取、不重算** ——
#     质检层一旦开始算数，就会出现"报告里的数和表里的数不一样"这种最难查的问题。
#   · `druid.io.handoff`：与结果表并排写 `<结果名>.handoff.json`
#     （schema `druid.handoff/1`），写明三件事：口径（`_1s` 是 1σ 不是 2σ）、
#     前提（外部重现性是实测还是假设、参考值自不自洽）、交接风险
#     （哪些测点交给 ADEPT 会被静默丢掉、为什么）。
#   · 命令行默认写这个 JSON（`--json PATH` 换路径、`--no-json` 不写），
#     并把质控结论打印在收尾处；网页界面同样写盘并登记成可下载产物。
#   · 交接文件**故意不给每个样品的加权平均年龄**，理由写在
#     `intentionally_omitted` 字段里（样品内测点散布常有池化 MSWD 10³~10⁴，
#     加权平均只能靠剔掉大部分测点达标）。要出岩样年龄，筛选标准得来自
#     地质证据（CL 图像、Th/U、协和度），不能来自 MSWD。
#   实测交叉验证：新检查项预测的"ADEPT 会丢哪些测点"与真跑 ADEPT 的记录
#   逐点一致（6 个真实批次，17/17）。
#
# 2.2.3 新增 docs/getting-started.html —— 面向"第一次拿到这个工具的人"的上手指南：
#   装环境四条命令、数据怎么摆、结果先看哪三个数、出错怎么定位、改代码的边界。
#   同时做了一批纯卫生修正（不改任何数值输出）：
#   · 删掉两个全仓无调用的死函数（io.qtegra.channels_of_interest、
#     depth.figures.multi_page_pdf）及其导出，连带删掉只被前者使用的 UA_MASSES；
#   · reduction.ratios 不再自带一份与 core.constants.MASSES_NEEDED 重复的通道元组；
#   · webui 的 HTTP `Server` 头不再硬编码 "druid-webui/2.0"，改读 __version__；
#   · 修正两处会把读者带偏的注释（QC 校正的 1σ/2σ、去掉扩展名为何不能用 rstrip）；
#   · io.report 的裸 except 收窄为 (AttributeError, TypeError, ValueError)。
#   新手指南里的每个数字都取自示例批次 EX2022A 的端到端回归基线，可自行复现。
#
# 2.2.2 新增 docs/from-first-principles.html —— 从物理原理讲到 MSWD 的完整说明。
#   纯文档改动，不动任何数值（端到端回归的基线值不变）。
#
# 2.2.1 修掉一个只在 Windows + 输出重定向时出现的崩溃：
#   本包的进度与摘要输出全是中文。把 stdout 重定向到文件/管道时，
#   Python 用 locale 编码（英文系统上是 cp1252）编码它，第一句
#   `print("...通过")` 就抛 UnicodeEncodeError 把进程带走 ——
#   而且是在跑完批处理、准备打印结果的时刻，最难受的时机。
#   现在 `druid/console.py` 在入口把输出流切成 UTF-8（errors="replace"）。
#   交互式控制台不一定会踩到（Python 对控制台用宽字符 API），
#   所以本地开发几乎遇不到 —— CI 的 windows job 是第一个撞上的。
#
# 2.0.0 起由原先的 _legacy/*.py 单文件脚本重构为分层子包，
# 旧脚本保留在 druid/_legacy/ 下仅供对照，不再参与运行。
#
# 2.1.0 起 `深度剖面域` 表增加 `MSWD` 与 `MSWD_概率` 两列。这两个数一直算得出来
# （`summarize_segments()` 内部就在调 `weighted_mean()`，它本来就返回 MSWD），
# 只是导出时被丢掉了。补上是为了让本工具与 R 端 ADEPT 的坪年龄口径能直接并列
# 对照 —— 两边都是"反比方差加权平均 + 卡方上尾概率"。只增加列，既有列与数值未动。
#
# 2.2.0 起：
#   · 序列表支持 CSV/TSV 文本格式（原来只认 .xls/.xlsx），优先于二进制格式查找。
#     两列几十行的东西用文本存才能 diff、才能在 review 里看懂。
#   · 随仓库附带示例批次 `examples/EX2022A/`（85 个测点，样品名匿名成 S01…S48，
#     标样保留），CI 用它做端到端数值回归 —— 合成数据覆盖不了"数值算得对"。
#   · 修掉 CLI 收尾摘要里的一处统计错误：`DataFrame.get()` 返回的是**整表**的那一列，
#     于是 35 个标样测点混进了"样品年龄"，中位 458.1 被抬成 460.9 Ma、
#     5–95% 区间从 420~644 撑成 333~1044 Ma。
#   · `_read_rows` 对 .csv/.tsv 改为显式指定分隔符，不再交给嗅探 ——
#     样品名里有空格（`SRM 612`）时嗅探可能误判，把样品名劈成两半。
