# ── 分析数据与工具输出 ───────────────────────────────────────────────
# 这是工具的默认输出目录，装的是某个批次的真实结果：原始 Qtegra CSV、
# 结果工作簿、深度剖面图。它们属于某一次分析，不属于这个工具。
#
# 这条规则是被事实验证过的，不是预防性的：本仓库的第一个版本曾把这些
# 文件一起纳管（98 个文件，且在初始提交里），等到准备公开时才发现，
# 只能重写全部历史把它们剔掉。
#
# 想留档就单独存档"原始批次目录 + 运行参数"，两者足以完整复现。
结果/
结果_*/
# 顺手兜住默认命名之外的输出：DRUID 的结果表都以 _U-Pb结果 结尾
*_U-Pb结果.xlsx

# ── 但 examples/ 是故意的例外 ────────────────────────────────────────
# 上面挡的是"某个批次的结果"，不是"数据"本身。
# `examples/EX2022A/` 是随工具附带的示例批次：85 个测点的原始 Qtegra CSV +
# 文本序列表，样品名已匿名成 S01…S48、标样原样保留。它**必须**进仓库：
#   1. 它是唯一能证明"这套代码在真实数据上算得对"的东西 —— CI 靠它做端到端
#      数值校验（合成数据只能覆盖边界，覆盖不了数值）；
#   2. 新环境装好后跑它一遍，就知道有没有装错东西。
# 所以不要把它当作"误入仓库的数据"删掉。

# ── PyInstaller 构建产物 ──────────────────────────────────────────────
# build_tmp 是 PyInstaller 的中间目录，dist 是最终 exe；两者都能从 launcher.py
# 重新生成（双击 打包成exe.bat），而且 dist/DRUID.exe 单个就近 200 MB。
build_tmp/
dist/

# ── Python 打包产物 ──────────────────────────────────────────────────
# `pip wheel .` / `pip install -e .` 会在工作区留下这两个目录（源码的完整副本），
# 它们不是源码，入库只会让每次改代码都多出一份假 diff。
build/
*.egg-info/

# ── Python 字节码 ────────────────────────────────────────────────────
__pycache__/
*.py[cod]

# ── 图件 ─────────────────────────────────────────────────────────────
# 这里出现的 PNG/PDF 全部由 matplotlib 从原始数据生成，体积大（约 19 MB）
# 且二进制无法比对；要留档请把原始批次数据与参数一起保存，而不是图本身。
*.png
*.pdf
# 例外：docs/og-image.png 是 Pages 站点的分享卡片（手工设计的资产，不是
# matplotlib 产物），必须进版本库，否则线上 og:image 会 404。
!docs/og-image.png

# ── 虚拟环境与编辑器 ─────────────────────────────────────────────────
.venv/
venv/
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# ── 临时文件 ─────────────────────────────────────────────────────────
*.tmp
*.bak
~$*
